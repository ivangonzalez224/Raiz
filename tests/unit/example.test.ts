import { describe, expect, it } from "vitest";

// Test de humo: confirma que el runner y el setup funcionan.
// Reemplazar por tests reales (ej. helpers de src/lib) según se agreguen.
describe("sanity check", () => {
  it("suma correctamente", () => {
    expect(1 + 1).toBe(2);
  });
});
